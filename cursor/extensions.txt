# Cursor marketplace IDs. Labels in the UI are in the comments.
# Re-run install.sh after Cursor is installed so these land on a new machine.

# PHP
bmewburn.vscode-intelephense-client
devsense.composer-php-vscode
devsense.intelli-php-vscode
devsense.phptools-vscode
devsense.profiler-php-vscode
mehedidracula.php-namespace-resolver
valeryanm.vscode-phpsab

# Twig (UI name: "Twig") -- NOT in Cursor's marketplace (marketplace.cursorapi.com
# has zero results for it), so install.sh's --install-extension always fails for
# this one. Sideload the VSIX from the real VS Code Marketplace instead -- see
# README's "Extensions install.sh can't install" section.
whatwedo.twig

# CSS / HTML
bradlc.vscode-tailwindcss
ecmel.vscode-html-css

# Theme / snapshots
# gerane.theme-sunburst also isn't in Cursor's marketplace -- same VSIX sideload
# as Twig above.
gerane.theme-sunburst
adpyke.codesnap

# GitHub / infra
github.vscode-github-actions
github.vscode-pull-request-github
hashicorp.terraform
anysphere.remote-ssh

# Other editors
anthropic.claude-code
